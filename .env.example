# Database
DB_NAME=tir
DB_USER=tir
DB_PASSWORD=tir_dev_pwd
DB_HOST=localhost
DB_PORT=5433

# JWT (we'll use this in Phase 2)
JWT_SECRET=change-me-to-a-long-random-string-min-256-bits
JWT_EXPIRY_HOURS=24
